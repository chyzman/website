export const idle = {
	tickIntervalMs: 500,
	graceMs: 500,
	fadeMs: 1500
};

export const network = {
	posSendIntervalMs: 20
};

export const debounce = {
	cursorTypeMs: 60,
	selectionMs: 100
};

export const cursor = {
	remoteOpacity: 0.7,
	iconScale: 20 / 164,
	perfectCursorMaxIntervalMs: 25
};
